[package]
name = "rackforge-firmware"
version = "0.0.1"
edition = "2024"
publish = false
description = "Offline-only bare-metal Rust scaffold for rackforge firmware research"

[profile.dev]
panic = "abort"

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = false
